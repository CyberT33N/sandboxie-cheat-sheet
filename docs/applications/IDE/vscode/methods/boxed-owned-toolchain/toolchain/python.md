# Python

## Role in this method

Python is a governed shared runtime surface in the boxed-owned-toolchain method.

Bootstrap resolves the selected shared Python version and mirrors it into the local box execution tree.

## Method-level boundary

This method document keeps only the orchestration view.

The Python-domain source of truth lives here:

- `docs\applications\programming-languages\python\architectures\boxed-owned-toolchain\overview.md`

## Related

- `docs\applications\IDE\vscode\methods\boxed-owned-toolchain\toolchain\general.md`
- `docs\applications\IDE\vscode\methods\boxed-owned-toolchain\bootstrap\shared-layout.md`
- `docs\applications\programming-languages\python\general.md`
