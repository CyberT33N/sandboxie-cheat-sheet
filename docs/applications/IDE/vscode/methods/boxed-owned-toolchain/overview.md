# Boxed-Owned Toolchain / Boxed Authoring

## Status

This area is currently being expanded from an earlier placeholder into the documentation area for the newer boxed-owned-toolchain / boxed-authoring direction.

The older "not fully isolated" label should not be treated as the final architecture name for the current fully boxed target state.

## Global CLI single source of truth

The generic CLI control-plane guidance is centralized here:

- `docs\cli\general.md`
- `docs\cli\start\general.md`
- `docs\cli\terminal\general.md`

These documents are the single source of truth for:

- starting a program in a specific box
- opening a terminal in a specific box
- generic command-passing boundaries

## VS Code-specific overlays

The VS Code area keeps only the method-specific overlay:

- `docs\applications\IDE\vscode\methods\boxed-owned-toolchain\cli\general.md`
- `docs\applications\IDE\vscode\methods\boxed-owned-toolchain\cli\start\general.md`
- `docs\applications\IDE\vscode\methods\boxed-owned-toolchain\cli\start\terminal.md`

## Historical note

The earlier placeholder reference remains relevant as background:

- https://github.com/sandboxie-plus/Sandboxie/issues/5235

