# Host-Sync PNPM

## Status

This is the secondary / legacy PNPM architecture path in this repository.

It is not the preferred default.

## Why this file exists

The PNPM area is being normalized toward an explicit `architectures\...` split.

This file is the architecture entrypoint for the host-sync variant.

## Current source of truth

The detailed current host-sync PNPM write-up still lives here:

- `docs\applications\programming-languages\node\package-manager\pnpm\host-sync\overview.md`

Use that document for the currently preserved materialization/shared-store legacy guidance.

## Related

- `docs\applications\programming-languages\node\package-manager\pnpm\general.md`
- `docs\applications\programming-languages\node\package-manager\pnpm\architectures\boxed-owned-toolchain\overview.md`
