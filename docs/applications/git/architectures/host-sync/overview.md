# Host-Sync Git

## Status

This is the secondary / legacy Git architecture path in this repository.

It is not the preferred default.

## Why this file exists

The Git domain is being normalized toward an explicit `architectures\...` split.

This file is the architecture entrypoint for the host-sync variant.

## Current scope

This file is the host-sync Git architecture entrypoint.

It preserves the host-visible / legacy Git path as a secondary reference only.

## Related

- `docs\applications\git\general.md`
- `docs\applications\git\architectures\boxed-owned-toolchain\overview.md`
