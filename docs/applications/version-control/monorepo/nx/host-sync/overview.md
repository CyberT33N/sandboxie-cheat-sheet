# Host-Sync NX

## Status

This file is kept as a compatibility landing page.

The architecture-folder source of truth now lives here:

- `docs\applications\version-control\monorepo\nx\architectures\host-sync\overview.md`

## Why this file still exists

Older repository references still point to this path.

Those references should now be migrated toward the `architectures\...` path, but this landing page remains so that existing links do not break during cleanup.




