# Boxed-Own-Toolchain NX

## Status

This file is kept as a compatibility landing page for the older `boxed-own-toolchain` spelling.

The architecture-folder source of truth now lives here:

- `docs\applications\version-control\monorepo\nx\architectures\boxed-owned-toolchain\overview.md`

## Why this file still exists

Older repository references still point to this path and spelling.

Those references should now be migrated toward the `architectures\boxed-owned-toolchain\...` path, but this landing page remains so that existing links do not break during cleanup.
