# Host-Sync Nx

## Status

This is the secondary / legacy Nx architecture path in this repository.

It is not the preferred default.

## Current source of truth

The detailed current host-sync Nx write-up still lives here:

- `docs\applications\version-control\monorepo\nx\host-sync\overview.md`

Use that document for the currently preserved shared-cache and host-sync history.

## Related

- `docs\applications\version-control\monorepo\nx\general.md`
- `docs\applications\version-control\monorepo\nx\architectures\boxed-owned-toolchain\overview.md`
